{
  "name": "veerakran-s-portfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && npm run check:bundles",
    "check:bundles": "node scripts/check-client-bundles.mjs",
    "preview": "astro preview",
    "test:e2e": "npm run build && playwright test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.1.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@playwright/test": "^1.61.1",
    "@types/three": "0.185.1",
    "typescript": "^5.7.0"
  }
}
